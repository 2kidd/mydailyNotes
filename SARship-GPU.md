## VLM-Qwen：

​	1.环境问题搞半天，版本冲突和不兼容（ex：pytorch。cuda版本之类的）！！！
​	2.输入图片时报错，数据加载未进行预处理，转化为官方大模型所采用的数据格式！！！
​	3.最终还是没有采用Qwen-VL，被折腾坏了，太难配置环境了
​	这个给我的忠告就是尽量固定所有包和版本，如果是大模型微调，大模型又比较新，那就直接使用最新的库和包应该没问题。

## LLaVA-1.5（7B）：

​	Thinking Step：
​		1.运行成功了，环境配置比Qwen-VL简单多了，一下就搞好了
​		2.但是为什么每次问同一张图片，输出的话一模一样，一个字都没变，按理说不应该是随机的吗？
​		3.解决了，因为我们最开始把do_sample=False，抑制了随机性，do_sample改成False，再把temperature=0.7（越大越随机，越小波动越小），top_p=0.9（在概率为90%以上的词里面随机选择）。但是直接检测出的结果让人大跌眼镜，与真实情况差了十万八千里
​		4.开始进行调教（Prompt Engineering），通过上下文学习（In-Context Learning），把SAR图像解译和成像原理（SAR专家知识）写进Prompt（提示词）里，未改变模型参数。比如：第一，给AI赋予专家角色；第二，定义目标，或者把专家常用知识告诉他等
​		5.进行微调LoRA（Low-Rank-Adaption），让他更懂专业术语，不用每次麻烦地写提示词，而且还能精确定位船只坐标。自己利用.xml文件（标签）以及原始图像生成文本描述对（对于VLM的数据集）
​		6.如何加载这个“SAR 专用”的 LoRA 权重
​		7.使用LoRA微调成功了，得到了模型输出结果文件
​		8.开始拼接原来的模型与微调后（LoRA）的模型结果，结果又遇到了版本冲突，依赖，包的问题，搞半天。
​		9.之后包、版本及库的问题解决了，因为使用旧版不行（不兼容，有些模块又没有），使用最新版本也不行（有些模块改名了或者删掉了），之后给必须必要的包，库指定相对应的黄金稳定版本，得以解决库、包版本冲突不兼容问题
​		10.之后拼接微调模型与底座模型时也折腾半天，后面终于发现是因为下载的底座模型在多次下载中有缓存，那个缓存文件损坏了？或者与当前的库版本不兼容的问题
​		11.之后重新新建一个notebook，全部重新配置了，全部采用最新的版本，拥抱最新，环境问题解决了，因为LLaVA（7B）比较新，所以环境（包、库）都采用最新的版本完全没问题。之后直接拼接微调权重模型，over，发现结果确实有了专家级水准个屁，不过从回答上来看，至少还是懂一些SAR专业知识了
​		12.部署该大模型。因为kaggle的output路径最多存20G的文件（在这个位置也卡了一会，由于存储空间不够，所以清理存储空间，先放内存，再放硬盘等等，不过最后拼接后的大模型还是采取直接上传到hugging face了），所以拼接后的大模型就不放在kaggle上了，使用在kaggle的notebook拼接大模型，并直接上传到hugging face上面。采用了两种方案部署，方案一是直接在代码的时候调用模型，方案二是搭建了一个网页应用（发链接给别人，别人也能用），其实有方案三，就是把模型下载到本地来跑（不过自己笔记本没显卡，就不折腾了）。方案一和方案二，都需要先把模型下载到 Kaggle 本地，然后由 Kaggle 的显卡进行计算。不过  不过不用担心，Kaggle 下载 Hugging Face 的东西非常快，而且不耗费你的流量，很快，亲身体验三分钟左右可以下载15G左右，diao
​		13.那我已经验证了kaggle能够跑基本的大模型（虽然参数量少），那我下去就应该准备“数据集”，好的“文本知识”，自己构建一些“高质量的数据集”，之后利用现成的大模型微调，构建专家级模型。
​		14.而且hugging face与kaggle配合无敌，速度很快，15G的文件两三分钟下载好
​		15.现在有几个问题了，我发现了我的这个微调模型胡说八道，我虽然传了一张sar图像进去，但是我问的问题是我想学习sar图像有关知识，请你告诉我学习方法。结果输出还是说那个图片里有一只船，他甚至还看到了一个人。离谱了。




	Problem：
		1.加载LLaVA模型时，未看到本地有下载的模型（权重）文件，不知道下载到了哪里？还是未下载？直接在云端调用的？👌
		2.我想部署微调模型，应该怎么部署？上次的那个就单独检测船只的yolo框船只的可以简单部署了，但这个大模型应该怎么部署？👌
		3.微调模型有时效果可以，有时胡说八道，他甚至能在那个卫星sar图像上看到有人？离谱，可能这个与我用的中文问的有关系？毕竟这个llava模型原生语言是英语，对中文支持不友好可能？所以看不懂中文语义？
		4.我做的这个语言视觉（多模态）模型能不能可以不必须输入图像呢，只输入自然语言，或者只输入图像呢？



	Kaggle使用教程：
		1.https://www.kaggle.com/code/cnlnpjhsy/kaggle-transformers/notebook   kaggle详解
		2.https://www.kaggle.com/discussions/product-feedback/355440           kaggle file system save？

